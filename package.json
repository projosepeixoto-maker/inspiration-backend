{
  "name": "inspiration-backend",
  "version": "1.0.0",
  "description": "Backend pour application Inspiration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
```

**Sauvegardez** (Cmd + S)

---

### B) Créer un fichier .gitignore

Dans Sublime Text, créez un nouveau fichier dans le dossier `backend` nommé `.gitignore` avec ce contenu :
```
node_modules/
.env
.DS_Store